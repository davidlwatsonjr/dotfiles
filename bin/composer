#!/bin/bash
if [ ! -f ~/bin/composer.phar ]; then
  [ ! -d ~/bin ] && mkdir ~/bin
  cd ~/bin
  curl -sS https://getcomposer.org/installer | php
fi

while [ ! -r composer.json -a `pwd` != '/' ]; do
 cd ..
done

php-cli ~/bin/composer.phar $@

